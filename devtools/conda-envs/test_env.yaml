name: phastff-test

channels:
  - conda-forge

dependencies:
  - python
  - pip

    # Testing
  - pytest

    # Core-deps
  - openff-toolkit >=0.14.3
  - openff-interchange >=0.3.17
  - openff-units
  - smirnoff-plugins
